import React from React;

const Empty = ({ onAd}) => {
  return (
    <main class="appointment__add">
      <img
        class="appointment__add-button"
        src="images/add.png"
        
      >
  )
}